// program pencarian data array menggunakan indexOf
function panggilIndexof() {
  var kota = ["jakarta","medan","bandung","palembang"]

  // untuk mencari data dalam array, jika data ada 0-length dan jika tidak ada maka akan bernilai -1

  return kota.indexOf("solo");
}

console.log(panggilIndexof());
